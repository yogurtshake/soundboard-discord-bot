-create bot clone specifically for testing? (would just need it locally)

-add bot message prefix to all messages? (something like "->")? would help distinguish between separate messages.

-fix issue of crash when bot is force moved between VCs by server admin?

-add discord developer instructions to readme?

-fix lower casing of sound file name of bot's file attachment when !download is called

-fix issue of !upload automatically adding underscores instead of spaces in file names